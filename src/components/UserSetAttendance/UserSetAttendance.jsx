import React from "react";

const UserSetAttendance = ({ headers, usersData }) => {
  console.log(usersData);
  console.log(headers);
  return <div>UserSetAttendance</div>;
};

export default UserSetAttendance;
